# Le2i data

The raw Le2i videos are intentionally excluded from Git. Point `--dataset-root`
or `configs/data/le2i.yaml` at the local directory containing the six scene
folders. Generated manifests and pose caches are written under `data/`.
